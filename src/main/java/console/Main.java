package console;

public class Main {

 public static void main(String[] args) {
        // ApplicationContext ctx = new ClassPathXmlApplicationContext("beans.xml");
        // DepartmentRepository repo = ctx.getBean(DepartmentRepository.class);
        // DepartmentService serv = ctx.getBean("deptService",DepartmentService.class);
        
        // // Department d = ctx.getBean("dept",Department.class);
        // // System.out.println(d.getClass().toString());
        // Department dept = new Department();
        // dept.setNom("Informatique");
        // // repo.save(dept);
        
        // Department dept1 = new Department();
        // dept1.setNom("Finances");
        // serv.saveDepartment(dept1);
        // System.out.println("Département enregistré avec ID: " + dept.getId());
    }        // finally {
        //     if (conn != null && !conn.isClosed()) {
        //         conn.close();
        //     }
        // }

    }